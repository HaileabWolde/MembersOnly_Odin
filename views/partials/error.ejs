<!-- views/partials/errors.ejs -->
<% if (locals.errors) {%>
  <ul>
    <% errors.forEach(function(error) { %>
      <li class="text-[#EA0063] italic"><%= error.msg %></li>
    <% }); %>
  </ul>
<% } %>